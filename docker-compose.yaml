version: '3.8'
services:
  app:
    build: .
    privileged: true
    logging:
      driver: "json-file"
      options:
        max-buffer-size: "4m"
        mode: "non-blocking"
    environment:
      TOKEN: "${TOKEN:-forregistrytoken}"
      TOKEN_ORG: "${TOKEN_ORG:-fororgregistry}"
      ORG: "${ORG:-fororgregistry}"
      DOCKER_TLS_CERTDIR: ""
